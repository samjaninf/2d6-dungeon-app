# Lambert (Agent-3) Spawn — 2026-02-28T21:02:45Z

**Task:** Fixed animation issues and added doubles celebration effects

**Output:** Animation trigger pattern corrected. Root cause: conditional rendering (@if) creates components with face already set, preventing change detection in OnParametersSetAsync.

Solution implemented:
- Always render Dice components, use CSS `display:none` to hide when face=0
- Initialize DiceResult with PrimaryDice = 0
- Ensures animation triggers when value changes from 0 to result

**Components Updated:**
- Combat.razor (working example pattern)
- NewRoomDialog.razor (fixed from conditional to always-render)
- All dice components using Animate="true" now follow correct pattern

**Status:** ✓ Complete

**Critical Pattern (for team memory):**
- ✓ DO: Always render Dice, use CSS visibility to hide
- ✗ DON'T: Conditional @if rendering (breaks animation detection)
